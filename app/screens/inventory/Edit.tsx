import { useNavigation } from "expo-router";
import { Text, View } from "react-native-ui-lib";


export default function Edit () {
    const navigate = useNavigation();
    return(

        <View paddingH-50 flex bg-grey50>
        
        </View>    
    );
}